import styles from "../styles/modules/Photo.module.scss";

const Photo = () => {
  return (
    <section className={styles.photo}>
      <div className={styles.photo__container}></div>
    </section>
  );
};

export default Photo;
